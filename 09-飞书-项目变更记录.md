# 飞书 · 项目变更记录

> 本文档记录本项目从提出到完成的全部变更过程，内容与项目开发沟通记录一致，可直接复制到飞书文档。

## 一、项目背景

项目名称：**轻量企业客服工单系统**

业务模型参考开源项目 PESCMS-Ticket（中小企业售后工单管理平台），最终实现为前后端分离的全新系统：

- 前端：Vue 3 + Element Plus + Vite
- 后端：NestJS + TypeORM + JWT + Socket.IO
- 数据库：MySQL（全新表结构，库名 `ticket_system`）

## 二、变更时间线

### 第 1 阶段：初始方案（第一版文档，PHP 版）

- 提供 PESCMS-Ticket 全套软件工程考核文档（项目概述、参与者、UML 图、用例规约、测试用例），共 8 份 Markdown 文档；
- 确认项目 GitHub 地址（`lazyphp/PESCMS-Ticket`，原提供的地址 404，已核实修正）；
- 克隆仓库并本地运行：安装 PHP 7.4、连接本机 MySQL 8（root/root），跑通安装向导与后台登录。

### 第 2 阶段：技术栈重写（放弃 PHP，改 Vue）

- 确认修改方向：**不要 PHP，改为 Vue**；
- 通过两轮"是/不是"问卷确认方案：
  - 前端 Vue 3 + Element Plus + Vite，全新界面；
  - 后端 NestJS（Node.js），完全移除 PHP；
  - 数据库保留 MySQL，但表结构全新设计（不再沿用旧 pes_ 表）；
  - 保留三角色（客户 / 客服专员 / 客服主管）及全部核心功能；
  - 补齐演示数据（分类、FAQ、公告、快捷回复、示例工单）；
  - 去掉开源版元素（应用商店、模板商店、授权检测、版权页脚）；
  - 部署保持简单（一键启动脚本），代码留扩展位；
  - 微信小程序源码暂不迁移，保留备用；
- 新建 `ticket-system/` 全栈项目：后端 8 个业务模块、前端三端页面、种子数据脚本；
- 端到端验证通过：客户建单 → 主管分配 → 客服回复 → 标记待确认 → 客户确认关闭，越权访问返回 403。

### 第 3 阶段：清理旧内容

- 停止原项目；
- 删除原 PHP 项目目录（PESCMS-Ticket，含 .git）；
- 删除旧数据库 `pescms`（保留新库 `ticket_system` 及其他数据库）；
- 删除 PHP 运行时（D:\tools\php74）与临时测试文件；
- 移除失效的旧运行说明文档。

### 第 4 阶段：新时代功能扩展（10 项）

通过问卷确认扩展方向，移动端与多租户暂缓：

1. **AI 智能客服**：回复建议、工单摘要、自动分类（未配置大模型时规则兜底）；
2. **知识库问答（RAG）**：客户提问按相似度检索 FAQ 并给出答案；
3. **实时在线会话**：Socket.IO 推送留言与状态变更；
4. **多渠道接入**：网页 / 微信 / 企业微信 / 邮件渠道配置 + 入站 webhook 建单；
5. **自动分配 + SLA**：按负载最少自动指派；按优先级计算响应/解决时限并标记超时；
6. **满意度评价**：工单完结后客户 1-5 分评价 + 评语（CSAT 统计）；
7. **数据看板**：14 天趋势、分类分布、客服绩效、响应/解决时长、CSAT；
8. **消息通知**：新工单 / 分配 / 回复 / 完结站内通知 + 未读角标；
9. **自定义表单**：主管可视化配置文本 / 多行 / 下拉 / 日期字段，客户提交时动态渲染；
10. **多语言**：简体中文 / English 切换并持久化；

全部功能经接口与页面双重验证通过。

### 第 5 阶段：纯视觉改造

反馈："界面太简陋、太工业化，希望个性化，但要一眼能找到功能。"

- 两轮视觉问卷确认：浅色明亮底 + 科技网格纹理 + 靛蓝紫主色（橙/青点缀）+ 切角面板 + 粗黑体标题 / 等宽数字 + 轻动效；保留左侧导航 + 顶栏结构；
- 参照灵感：明日方舟 / 女神异闻录的"个性化 UI"（不照搬，只提取切角、几何、独特字体等轻量元素）；
- 实施内容：切角徽标与面板、网格背景纹理、渐变欢迎横幅、彩色图标统计卡（数字跳动）、渐变图表、头像气泡聊天、左右分栏登录页、可折叠侧边栏、深色模式、页面过渡动画。

### 第 6 阶段：交互与可用性修正

1. **"快速开始"按钮真实可用**：用户端横幅的 GO 卡片从纯展示改为真按钮，点击跳转提交工单；
2. **光标与文字选择规范**：不可选文字统一默认箭头光标，输入框保持文本光标，可交互元素显示手型，仅留言/答案/正文可选中复制；
3. **客服端内容补足**：新增"待处理工单""最新客户留言""快捷回复（点击复制）"面板，后端新增工作台数据接口（`GET /tickets/staff-feed`）；
4. **按钮感内容全面排查**：统计卡与横幅数据卡全部可点击跳转对应筛选列表；"平均满意度"等无跳转页面的卡片改为纯展示样式（虚线、无悬停、无手型）；登录/注册表单卡移除误导性悬停；工单列表支持 URL 参数直达筛选（`?status=pending`、`?overdue=1`）。

## 三、当前系统状态

| 项目 | 状态 |
| --- | --- |
| 代码 | `项目实践/ticket-system/`（server + web） |
| 数据库 | MySQL `ticket_system`，14 张表，演示数据完整 |
| 文档 | 项目实践目录 00-08（最终版） |
| 运行 | 双击 `start.bat` 启动，`seed.bat` 初始化数据 |
| 演示账号 | manager / staff01 / staff02 / customer01 / customer02（密码 123456） |

## 四、遗留与后续（暂缓项）

- 移动端（小程序 / PWA / App）：接口已统一 `/api/v1`，可复用；
- 多租户 SaaS 化：角色与数据归属字段已预留；
- 微信 / 企业微信 / 邮件真实凭证接入：webhook 结构已就绪，需补充签名校验；
- 大模型正式接入：AI 配置页填写接口与 Key 即可启用（当前为规则兜底）。
